__pycache__/
*.pyc
*.pyo
*.pyd
data/*
.Python
env/
venv/
.venv/
*.egg-info/
*.egg
dist/
build/
*.sqlite3
*.db
.env
# uv.lock - committed for reproducible CI builds

# Test and runtime generated data
data/backup/
data/sessions/
data/transactions/HEAD
data/canonical_path.json
# Specific database files if not covered by *.db, e.g. data/encyclopedia.duckdb
# However, *.db should cover it.
# Test output files in the_library if any remain despite fixes (should be temporary now)
# Example: the_library/test_*.md

# Un-ignore the main DuckDB database file
!data/encyclopedia.duckdb

# Gemini CLI
.gemini/
gha-creds-*.json
